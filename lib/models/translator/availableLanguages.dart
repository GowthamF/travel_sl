class AvailableLanguage {
  final int id;
  final String languageShortName;
  final String displayName;
  bool isSelected;

  AvailableLanguage(
      {this.id, this.languageShortName, this.displayName, this.isSelected});
}
